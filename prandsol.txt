1. Как черт возьми делать выравнивание в логотипе? Т.е. внутри элемента float?
2. Как сделать все таки эту чертову полосочку? 
3. Секция с преимуществами, не получается сделать картинки с использованием псевдоэлементов. Только как background
4. Как избежать пробела, который появляется при масштабировании между секциями с backgroud я вроде разобрался.
5. Как прописывать outline чтобы не было синей обводки вокруг input.


// Картинки где отзывы: извлечь фотку целиком, положить ее в div с overflow:hidden с заданным размером и двигать ее при помощи position:relative.

Если добавляешь линию при помощи псевдоэлемента - она получается внутри паддинга, не на границе элемента

Не получается изменить цвет для грузовичка в логотипе.

Подключение календаря при помощи jqueryui - выбор datapicker.


Пагинацию сделать ul>li






